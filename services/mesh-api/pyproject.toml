[project]
name = "stepwise-mesh-api"
version = "0.1.0"
description = "FastAPI mesh recovery service for Stepwise"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.104", "pydantic>=2", "uvicorn>=0.24", "boto3>=1.34"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
