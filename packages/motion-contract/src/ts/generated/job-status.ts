/**
 * GENERATED FILE — do not edit by hand.
 * Source of truth: packages/motion-contract/schema/*.schema.json
 * Regenerate with: npm run generate
 */

/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Deliberately separate from MotionResult (which only ever describes a finished job). services/motion-api polls/pushes this while a job is queued, processing, or failed; once `state` is "succeeded", the job's MotionResult document is fetched separately by job_id. Keeping these two contracts apart means a MotionResult document is never in a partially-valid state.
 */
export interface JobStatus {
  schema_version: "1.0.0";
  /**
   * Same immutable id that will appear as MotionResult.job_id once the job succeeds.
   */
  job_id: string;
  /**
   * "succeeded" and "failed" are terminal. A retried job stays at job_id and moves back to "queued", incrementing retry_count, rather than minting a new id.
   */
  state: "queued" | "processing" | "succeeded" | "failed";
  /**
   * Plain-language current stage for display, per DESIGN.md §7c — e.g. "Building the body — count 9 of 32". Never a bare percentage or an internal stage enum. Empty string when state is "queued".
   */
  stage_message: string;
  /**
   * Rough fraction complete, for a progress bar's width only. DESIGN.md §7c: display as loose relative time ("about 2 minutes left"), never as false-precision countdown. null when state is "queued" or "failed".
   */
  progress: null | number;
  /**
   * Required to be null unless state is "failed".
   */
  error: null | {
    /**
     * Stable machine-readable code, e.g. "no_dancer_found", "clip_too_long", "pipeline_error". Drives which OPEN-DECISIONS.md B1/B2 recovery UI is shown.
     */
    code: string;
    /**
     * Human-readable, says what happened — never blame the user, never apologize (DESIGN.md §11).
     */
    message: string;
    /**
     * Whether re-submitting the same clip could plausibly succeed (true for transient GPU/queue errors, false for e.g. "no dancer found").
     */
    retryable: boolean;
  };
  /**
   * Number of times this job has been retried. 0 for a job's first attempt.
   */
  retry_count: number;
}
