# Stepwise Frontend Environment Variables

# URL of the processing pipeline (default: http://localhost:8000)
PIPELINE_URL=http://localhost:8000

# Set to "true" to use mock data instead of the real pipeline
# USE_MOCK=true
